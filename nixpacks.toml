[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node --import tsx scripts/migrate.ts && NODE_ENV=production node --import tsx server/index.ts"
