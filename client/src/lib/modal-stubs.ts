    private openSettingsModal() {
        if ((window as any).openSettingsModal) {
            (window as any).openSettingsModal();
        }
    }

    private openHistoryModal() {
        if ((window as any).openHistoryModal) {
            (window as any).openHistoryModal();
        }
    }

    private openControlsModal() {
        if ((window as any).openControlsModal) {
            (window as any).openControlsModal();
        }
    }
